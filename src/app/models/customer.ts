export interface Customer {
  customerId: number;
  companyName: string;
}

export interface CustomerDetails {
  name: string;
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  status: boolean;
  companyName: string;
}
